test:
  service: Disk
  root: <%%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%%= Rails.root.join("storage") %>

amazon:
  service: S3
  access_key_id: <%%= ENV['S3_ACCESS_KEY'] %>
  secret_access_key: <%%= ENV['S3_SECRET_KEY'] %>
  region: us-east-1
  bucket: <%= app_name.tr('_', '-') %>-<%%= Rails.env %>
